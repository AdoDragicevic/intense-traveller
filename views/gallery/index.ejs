<%- include("../partials/header") %>


<h1>Picture Gallery</h1>

<a href="/gallery/new">New</a>

<h3>Albums:</h3>

<% galleries.forEach(function(gallery){ %>
	<li> <a href="/gallery/<%= gallery._id %>"> <%= gallery.title %> </a> 
		<% let numOfLikes = 0; %>
		<% gallery.imgs.forEach(function(img, i){ %>
			<% numOfLikes = numOfLikes + img.likes.length; %>
		<% }); %>
		<%= numOfLikes %> <i class="fas fa-thumbs-up"></i> 
		<a href="/profile/<%= gallery.author.id %>"> <%= gallery.author.username %> </a>
	</li>
<% }); %>


<%- include("../partials/footer") %>


